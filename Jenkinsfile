pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/Rehtegot47/pipiline.git', branch: 'main'
            }
        }
    }
}